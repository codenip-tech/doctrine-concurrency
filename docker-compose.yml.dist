version: '3.5'

services:
  doctrine-concurrency:
    container_name: doctrine-concurrency
    build:
      context: ./docker
      args:
        UID: $U_ID
    volumes:
      - ./:/var/www/html
    ###> XDEBUG 3 ###
    # Use your client IP here
    # Linux: run "ip a | grep docker0"
    # Windows (with WSL2) and Mac: host.docker.internal
    environment:
      XDEBUG_CLIENT_HOST: 172.17.0.1
      XDEBUG_CLIENT_PORT: 9003
      PHP_IDE_CONFIG: serverName=symfony-server
    ports:
      - '1000:80'
    networks:
      - doctrine-concurrency-network
    depends_on:
      - doctrine-concurrency-mysql

  doctrine-concurrency-mysql:
    container_name: doctrine-concurrency-mysql
    image: mysql:8.2.0
    ports:
      - '3336:3306'
    environment:
      MYSQL_DATABASE: doctrine_concurrency
      MYSQL_ROOT_PASSWORD: root
    volumes:
      - doctrine-concurrency-mysql-data:/var/lib/mysql
    networks:
      - doctrine-concurrency-network
    command: [ 'mysqld', '--character-set-server=utf8mb4', '--collation-server=utf8mb4_unicode_ci' ]

networks:
  doctrine-concurrency-network:
    name: doctrine-concurrency-network

volumes:
  doctrine-concurrency-mysql-data:
